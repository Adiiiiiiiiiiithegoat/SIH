# Copy this file to .env and fill in the real value. .env is gitignored --
# never commit it. On a hosted deployment, set this as an environment
# variable in the platform's dashboard/secrets instead of shipping a .env.
DEEPSEEK_API_KEY=
